
&НаКлиенте
Процедура ОбработатьПовторно(Команда) 
	Отказ = Ложь;  
	ТекстОшибки = "";
	TrierRabbitMQОбщийМодульВызовСервера.ОбработкаПолученногоСообщения(Запись.Сообщение,Отказ,ТекстОшибки);
	Запись.Ошибка = ТекстОшибки;
	Сообщить(ТекстОшибки);
КонецПроцедуры
